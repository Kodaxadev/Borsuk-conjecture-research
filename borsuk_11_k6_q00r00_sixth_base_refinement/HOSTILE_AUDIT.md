# Hostile audit

## Pointwise stabilizer substituted for set stabilizer

Rejected. The unordered stabilizer has order 24 and gives 36 orbits. The pointwise subgroup has order 2 and gives 222 orbits. Both values are recomputed and checked.

## Missing affine automorphisms

Rejected. Once a proposed base permutation fixes the translation, every coordinate permutation solving the four image equations is enumerated through all bijections of equal column-pattern classes.

## Stale candidate input

Rejected. Neither implementation reads a parent trim, candidate list, assignment file, or the other implementation's output. Both scan the full cube directly.

## Signature-only orbit classification

Rejected. Orbits are generated by applying every explicit stabilizer element. Every representative-to-member map is recorded and re-evaluated.

## Gaps or overlaps

Rejected. Coverage multiplicity must be exactly one for all 329 candidates and zero otherwise.

## Status leakage

Rejected. All 36 child identifiers remain `UNKNOWN`; no child trim or theorem promotion is produced.
