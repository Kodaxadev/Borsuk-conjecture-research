# Status and logical scope

**Certified computational statement:** the 692-vertex universal trim graph is not 12-colorable.

**Unresolved theorem statement:** every actual diameter-6 subset of the 11-cube is 12-colorable.

The gap between these statements is essential. Actual diameter-6 subsets must avoid all vertex pairs at distance greater than 6. The trim graph ignores those mutual compatibility constraints after fixing only 0 and one distance-6 neighbor A.

Therefore the UNSAT certificate redirects the attack rather than ending it: classify or branch on a second normalized vertex B and color the further trim T intersect B_6(B), or find a more compressed equivalent classification.
